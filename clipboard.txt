python3 << 'PYEOF'
with open('/home/user/cloud-screen/main.py', 'r') as f:
    content = f.read()

# 修复1: paste_to_doc 去掉多余步骤
old = """def paste_to_doc():
    subprocess.run(['xdotool', 'windowactivate', CHROME_WID], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'key', 'ctrl+2'], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'type', 'Google'], env=ENV)
    time.sleep(0.5)
    subprocess.run(['xdotool', 'key', 'Return'], env=ENV)
    time.sleep(0.8)
    subprocess.run(['xdotool', 'key', 'ctrl+End'], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'key', 'Return'], env=ENV)
    time.sleep(0.2)
    subprocess.run(['xdotool', 'key', 'ctrl+v'], env=ENV)
    print('已粘贴到Doc')"""

new = """def paste_to_doc():
    subprocess.run(['xdotool', 'windowactivate', CHROME_WID], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'key', 'ctrl+2'], env=ENV)
    time.sleep(0.8)
    subprocess.run(['xdotool', 'key', 'ctrl+End'], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'key', 'Return'], env=ENV)
    time.sleep(0.2)
    subprocess.run(['xdotool', 'key', 'ctrl+v'], env=ENV)
    print('已粘贴到Doc')"""

content = content.replace(old, new)

# 修复2: 新增 type_enter 命令
old2 = """            elif cmd.get('type') == 'type':
                type_text(cmd.get('text', ''))"""

new2 = """            elif cmd.get('type') == 'type':
                type_text(cmd.get('text', ''))
            elif cmd.get('type') == 'type_enter':
                type_text(cmd.get('text', ''))
                time.sleep(0.3)
                subprocess.run(['xdotool', 'key', 'Return'], env=ENV)
                print('发送+Enter')"""

content = content.replace(old2, new2)

with open('/home/user/cloud-screen/main.py', 'w') as f:
    f.write(content)
print('完成 ✅')
PYEOF