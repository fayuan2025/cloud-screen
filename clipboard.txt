python3 << 'PYEOF'
with open('/home/user/cloud-screen/main.py', 'r') as f:
    content = f.read()

old = """            elif cmd.get('type') == 'key':
                subprocess.run(['xdotool', 'key', '--clearmodifiers', cmd.get('key', '')], env=ENV)
                print('按键: ' + cmd.get('key', ''))"""

new = """            elif cmd.get('type') == 'key':
                subprocess.run(['xdotool', 'windowactivate', CHROME_WID], env=ENV)
                time.sleep(0.2)
                subprocess.run(['xdotool', 'key', '--clearmodifiers', cmd.get('key', '')], env=ENV)
                print('按键: ' + cmd.get('key', ''))"""

content = content.replace(old, new)
with open('/home/user/cloud-screen/main.py', 'w') as f:
    f.write(content)
print('完成 ✅')
PYEOF