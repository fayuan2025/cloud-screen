修复成功！看到：
- `:0` ✅
- `ctrl+2` ✅
- 没有 `ctrl+shift+a` ✅

现在重启 main.py：

```bash
pkill -f main.py && sleep 1 && cd ~/cloud-screen && source venv/bin/activate && python3 main.py &
```

然后测试：
1. 云电脑网页上复制一段文字
2. 本机点 **"📄 写入Doc"**
3. 看云电脑 Doc 标签页有没有粘贴成功

告诉我结果 ✅