python3 << 'PYEOF'
import subprocess, time, os

ENV = dict(os.environ)
ENV['DISPLAY'] = ':0'

# 放入测试文字
p = subprocess.Popen(['xclip', '-selection', 'clipboard'], stdin=subprocess.PIPE, env=ENV)
p.communicate(input='测试粘贴999'.encode('utf-8'))
time.sleep(0.5)

# 先切到第2个标签，再粘贴
subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', 'ctrl+2'], env=ENV)
time.sleep(0.8)
subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', 'ctrl+v'], env=ENV)
print('完成，看 Doc 有没有出现 测试粘贴999')
PYEOF