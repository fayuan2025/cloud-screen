python3 << 'PYEOF'
with open('/home/user/cloud-screen/main.py', 'r') as f:
    content = f.read()

old = """            elif cmd.get('type') == 'key':
                subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', cmd.get('key', '')], env=ENV)
                print('按键: ' + cmd.get('key', ''))"""

new = """            elif cmd.get('type') == 'key':
                subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', cmd.get('key', '')], env=ENV)
                print('按键: ' + cmd.get('key', ''))
            elif cmd.get('type') == 'paste_tab2':
                subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', 'ctrl+2'], env=ENV)
                time.sleep(0.8)
                subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', 'ctrl+v'], env=ENV)
                print('粘贴到Doc ✅')"""

content = content.replace(old, new)
with open('/home/user/cloud-screen/main.py', 'w') as f:
    f.write(content)
print('完成 ✅')
PYEOF