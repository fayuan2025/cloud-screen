python3 << 'PYEOF'
with open('/home/user/cloud-screen/main.py', 'r') as f:
    content = f.read()

# 修复1: type_text 增加延迟
old = """def type_text(text):
    subprocess.run(['xdotool', 'search', '--class', 'chrome', 'type', '--clearmodifiers', '--delay', '20', text], env=ENV)
    print('输入: ' + text[:20])"""

new = """def type_text(text):
    subprocess.run(['xdotool', 'search', '--class', 'chrome', 'type', '--clearmodifiers', '--delay', '50', text], env=ENV)
    print('输入: ' + text[:20])"""

content = content.replace(old, new)

# 修复2: type_enter 的 Return 键
old2 = """            elif cmd.get('type') == 'type_enter':
                type_text(cmd.get('text', ''))
                time.sleep(0.3)
                subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', 'Return'], env=ENV)
                print('发送+Enter')"""

new2 = """            elif cmd.get('type') == 'type_enter':
                type_text(cmd.get('text', ''))
                time.sleep(0.5)
                subprocess.run(['xdotool', 'search', '--class', 'chrome', 'key', '--clearmodifiers', 'Return'], env=ENV)
                print('发送+Enter')"""

content = content.replace(old2, new2)

with open('/home/user/cloud-screen/main.py', 'w') as f:
    f.write(content)
print('完成 ✅')
PYEOF