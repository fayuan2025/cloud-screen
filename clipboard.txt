user@ubuntu-2gb-ash-1:~$ python3 << 'PYEOF'
with open('/home/user/cloud-screen/main.py', 'r') as f:
    content = f.read()

# 修复1: DISPLAY
content = content.replace("os.environ['DISPLAY'] = ':0.0'", "os.environ['DISPLAY'] = ':0'")

# 修复2: paste_to_doc 整个函数
old = """def paste_to_doc():
    subprocess.run(['xdotool', 'windowactivate', CHROME_WID], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'key', 'ctrl+shift+a'], env=ENV)"""

new = """def paste_to_doc():
    subprocess.run(['xdotool', 'windowactivate', CHROME_WID], env=ENV)
    time.sleep(0.3)
    subprocess.run(['xdotool', 'key', 'ctrl+2'], env=ENV)"""

content = content.replace(old, new)

with open('/home/user/cloud-screen/main.py', 'w') as f:
    f.write(content)

PYEOF("完成 ✅")out)n.py'], capture_output=True, text=True)ctrl+shift", '/home/u
9:os.environ['DISPLAY'] = ':0'
88:    subprocess.run(['xdotool', 'key', 'ctrl+2'], env=ENV)

完成 ✅
user@ubuntu-2gb-ash-1:~$